[phases.setup]
aptPkgs = ["ffmpeg", "gcc", "libopus-dev", "python3-dev"]

[phases.install]
cmds = [
  "pip install --no-cache-dir -r requirements.txt",
  "pip install --no-cache-dir --force-reinstall pytgcalls==2.3.3"
]

[start]
cmd = "python bot.py"
