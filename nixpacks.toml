[phases.setup]
aptPkgs = ["ffmpeg", "gcc", "libopus-dev", "python3-dev"]

[phases.install]
cmds = [
  "pip install -r requirements.txt --no-cache-dir",
  "pip install py-tgcalls --upgrade --no-cache-dir"   # ← أضف السطر ده
]

[start]
cmd = "python bot.py"
